// Bumped to 2 for M2: round.* intents, a widened `results`, and CELL_LOCKED.
// The gate exists to kill stale tabs after a deploy — an M1 client cannot play
// an M2 game.
export const PROTOCOL_VERSION = 2;
